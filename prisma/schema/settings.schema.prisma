model Settings {
  id                 Int      @id @default(1)
  free_trial_months  Int      @default(2)
  subscription_price Int      @default(50000)
  updatedAt          DateTime @updatedAt

  @@map("settings")
}
