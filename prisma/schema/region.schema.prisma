model Region {
  id    Int     @default(autoincrement()) @id
  name  String?
  image  String?
  shops Shop[]
  ads Ad[]
  createdt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
}