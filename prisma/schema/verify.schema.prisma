model Verify {
  id    Int     @default(autoincrement()) @id
  phone  String?
  code  String?
  expired DateTime?

  createdt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
}